export type TRecipe = {
    id: string;
    title: string;
    image: string;
    time: string;
    servings: string;
    calories: string;
    description: string;
    ingredients: [string];
    instructions: [string];
    link: string;
}